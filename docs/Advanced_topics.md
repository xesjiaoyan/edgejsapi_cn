# Advanced topics

##一个页面中多个作品
现在Edge运行时支持一个页面中多个作品,但是Edge Animate2.0不支持一个页面多个作品。
想要在一个页面中包含多个作品,需要按照以下操作手动编辑。

每个AdobeEdge.loadComposition调用导致一个独立的作品包含在一个页面。
你可以通过AdobeEdge.loadComposition调用更多的作品来包含多个作品。

